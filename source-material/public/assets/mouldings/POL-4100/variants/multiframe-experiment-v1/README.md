# POL-4100 multi-frame experiment v1

This directory is reserved for supplier-only multi-frame reconstruction.

It must not overwrite `../baseline-v1/`. Experimental maps remain inactive
until explicitly selected and accepted in the asset-review interface.
